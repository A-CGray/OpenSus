function plotPoints(inp,varargin)
    plotfcn = p@(x)drawSphere(x,10, 'facecolor', 'r');
    
    % Figure out if user has input a corner structure, a front or rear
    % structure or the whole geom structure
end